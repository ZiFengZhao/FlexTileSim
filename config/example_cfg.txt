# ==========================================================
#  Example NPU Simulator Configuration File
# ==========================================================

# ---- Core Configuration ----
core_num = 2                   # number of cores
npu_freq = 3.2                 # NPU Core frequency (GHz)
systolic_array_size = 16       # systolic array dimension (16x16)
dma_bus_width = 4096           # DMA transfer bus width (bytes)
inst_buffer_depth = 4          # instruction buffer depth
dma_queue_depth = 2            # DMA queue depth
ce_queue_depth = 2             # CE queue depth
ce_cycle_factor = 1            # CE cycle factor
ce_cycle_bias = 2              # CE cycle bias

# ---- NoC Configuration ----
noc_mode = 1                   # NoC type: 1 (analytic) or 0 (detail)
booksim_cfg_path = ./external/Booksim2/src/examples/mesh22_lat # Booksim configuration file path
noc_freq = 3.2                 # NoC frequency (GHz)
noc_link_width = 64            # NoC link width (bytes)
noc_link_latency = 1           # NoC link latency (cycles)
noc_router_latency = 1         # NoC router latency (cycles)
noc_mesh_width = 2             # number of routers in x direction
noc_mesh_height = 1            # number of routers in y direction
noc_request_queue_depth = 4    # NoC request queue depth
an_noc_method = 2              # analytic NoC method: 2 (traffic-aware) or 1 (queueing with fixed parameters) or 0 (hop-based)           
noc_sampling_period = 1000     # NoC sampling period
noc_num_saved_samples = 5    # maximum number of samples in sliding window

# ---- DRAM Configuration ----
ddr_mode = 1                   # DRAM type: 1 (analytic) or 0 (detail)
dramsim_cfg_path = ./external/DRAMsim3/configs/DDR4_8Gb_x8_3200.ini # DRAMSim3 configuration file path, only valid in detail dram mode
ddr_freq = 3.2                 # DRAM frequency (GHz)
ddr_bandwidth = 21.3           # DRAM bandwidth (GB/s) 
tCAS = 15                      # DRAM CAS latency (DDR cycles)
tRCD = 15                      # DRAM RCD latency (DDR cycles)
tRP = 15                       # DRAM RP latency (DDR cycles)
tBURST = 4                     # DRAM burst latency (DDR cycles)
BL = 8                         # DRAM burst length (typically 8 bits)
ddr_bus_width = 64             # DRAM bus width (typically 64 bits)
ddr_channel_num =  1           # DRAM channel number 
ddr_request_quue_depth = 8     # DRAM request queue depth

# ---- Simulation parameters ----
max_sim_cycle = 10000000       # maximum simulation npu core cycles
inst_file = ./benchmarks/example_gemm_dual_core.bin
enable_log = 1                 # enable log file: 1 (enable) or 0 (disable)
enable_inst_trace = 1          # enable instruction trace: 1 (enable) or 0 (disable)
log_dir = ./log
log_file   = example.log

